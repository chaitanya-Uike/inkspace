package views

templ HomePage(title string) {
	@Layout(title) {
		<div class="max-w-2xl mx-auto p-8">
			<h1 class="text-4xl font-bold text-gray-900 mb-4">{ title }</h1>
			<p class="text-gray-600">Hello from templ + Go! + websockets</p>
		</div>
	}
}
